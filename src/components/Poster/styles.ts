import {styled} from "@mui/material";
import { Stage } from "react-konva";

export const PosterContainer = styled(Stage)({
   // border: '2px solid gray',
    display: 'flex',
    alignItems: 'center',
    backgroundColor: '#e6eaec'
})